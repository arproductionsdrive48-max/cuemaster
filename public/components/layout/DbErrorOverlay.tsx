/**
 * DbErrorOverlay — DEPRECATED.
 * Replaced by FullScreenErrorOverlay + ConnectionContext.
 * Kept as empty re-export to avoid breaking any remaining imports.
 */
const DbErrorOverlay = () => null;
export default DbErrorOverlay;
